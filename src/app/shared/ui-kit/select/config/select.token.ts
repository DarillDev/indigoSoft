import { InjectionToken } from '@angular/core';
import { IFormFieldSelect } from '../interfaces/select.interface';

export const FORM_FIELD_SELECT = new InjectionToken<IFormFieldSelect>('FORM_FIELD_SELECT');
